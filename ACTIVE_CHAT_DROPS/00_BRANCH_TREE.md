# ESO BRANCH TREE

Status: `TREE_REGISTRY / ACTIVE`

This workspace is a research tree.

Each branch gets the same shape:

`00_START_HERE.md`
`ACTIVE`
`MEDIA`
`ARCHIVE`

Each branch gets the same working method:

`33rd Triple Pass: Walls -> Ceiling -> Roof -> Whole-House Floor Run`

Each branch keeps:

`active object`
`branch status`
`source routes`
`method cards`
`next run board`
`no-loss receipt`
`archive custody`

## Branches

| Branch folder | Branch object | Status | Next action |
| --- | --- | --- | --- |
| `HALL_COOP_LEY` | `AMERICA_ASSIGNMENT_MAP_BRANCH_A` | `ACTIVE / CLEAN_START` | `RETRACE-RUN-002 / ROUND 2 - RED-MARKER MAP FAMILY SPLIT` |
| `GROVE` | `BOHEMIAN_MARKER_LODGE_NODE_BRANCH` | `ACTIVE / CLEAN_START` | `BATCH A1.2 - EARLY ADDRESS LOCK` |

## Tree Rule

Do not mix branches.

Do not let archive files steer the active branch unless auditing or recovering exact old wording.

Do not promote riddle pressure, date pressure, map geometry, or symbolic comparison beyond source custody.

When a new investigation starts, make it a new branch folder with the same shape.

