# MULE HANDOFF — HOUSE DOCK CONTROL ROOM SEED BUILD

Date: 2026-05-31  
Status: LOCAL BUILD HANDOFF / MULE CAN CHOOSE SAFE HOME / NOT DOCTRINE / NOT GIT SAVE  
PacketId: MULE_HANDOFF_HOUSE_DOCK_CONTROL_ROOM_SEED_BUILD_20260531

## Message to mule

We are working on the next organ of the house:

`HOUSE_DOCK_CONTROL_ROOM`

The dilemma:

We do not just need another script. We need a small local operating surface that can safely find, open, inspect, dry-run, run, prove, park, request, and later grow tools. The UI is not the important part yet. The important missing part is the control spine:

`selected command -> registry row -> tool card -> action policy -> power pocket -> run mode -> runner -> receipt -> event row -> current workbench update -> stop line`

This missing spine is called:

`HOUSE_DOCK_KERNEL`

The screen is the face. The kernel is the nervous system. The registry is the spine. The policy gate is the immune system. Receipts/event rows are memory. Current workbench is awareness. Request forge is growth. Factory nursery is future power with brakes.

## What mule may choose

Mule may choose the safest local staging home for the build.

Recommended default if suitable:

`C:\Users\13527\Desktop\123\HOUSE_DOCK_CONTROL_ROOM`

But mule may choose a better safe location if local layout demands it.

If mule chooses a different location, return the exact chosen root and reason.

## Hard ignore / do not disturb zones

Do not modify, move, delete, rename, or reorganize these unless explicitly told later:

- `C:\Users\13527\Desktop\HELPER_CONTEXT_AUTHORITY_PROOF_RESEARCH_PACK_20260531`
- Any existing research pack source files
- Any existing accepted batch files
- Any existing manifests
- Any Git repo files
- Any `ACTIVE_GUIDES`
- Any `CURRENT_TRUTH_INDEX`
- Any doctrine files
- Any watcher/automation files
- Any broad house ledgers not directly created by this task

Do not clean the research pack. It was already organized. This handoff is not a cleanup job.

## Boundary

Local-only build seed.

No Git writes.
No commit.
No push.
No watcher.
No background automation.
No whole-house scan.
No graph database.
No browser server.
No PowerShell Universal.
No Electron/Tauri.
No self-running tool factory.
No generated tool should become active automatically.
No delete/move of existing user files.

Unknowns go to review, never delete.

## What to build

Build the seed for:

`HOUSE_DOCK_CONTROL_ROOM`

Minimum folder shape:

```text
HOUSE_DOCK_CONTROL_ROOM
  00_READ_FIRST
    README_HOUSE_DOCK_CONTROL_ROOM_V1.md
    CURRENT_DOCK_CARRY_CARD.md

  START
    START_HOUSE_DOCK_MENU_V1.ps1
    START_HOUSE_DOCK_GRID_V1.ps1

  KERNEL
    HOUSE_DOCK_KERNEL_V1.ps1
    HOUSE_DOCK_ACTION_CONTRACT_V1.md

  REGISTRY
    TOOL_REGISTRY_V1.json
    PACK_REGISTRY_V1.json
    ACTION_POLICY_V1.json
    PATH_REGISTRY_V1.json
    SCHEMA
      TOOL_REGISTRY_SCHEMA_V1.json
      TOOL_CARD_SCHEMA_V1.json
      TOOL_REQUEST_SCHEMA_V1.json
      ACTION_CONTRACT_SCHEMA_V1.json

  TOOL_CARDS
    ORGANIZE_PACK_TOOL_CARD_V1.md
    SCENT_FINDER_TOOL_CARD_V1.md
    RECEIPT_MICROSCOPE_TOOL_CARD_V1.md
    PACK_DOCTOR_TOOL_CARD_V1.md
    HASH_LENS_TOOL_CARD_V1.md
    REQUEST_FORGE_TOOL_CARD_V1.md

  TOOLS
    SCENT_FINDER_V1.ps1
    RECEIPT_MICROSCOPE_V1.ps1
    PACK_DOCTOR_V1.ps1
    HASH_LENS_V1.ps1
    REQUEST_FORGE_V1.ps1

  RECEIPTS
  RUN_LOGS

  EVENT_LEDGER
    HOUSE_DOCK_EVENT_LEDGER.jsonl
    CURRENT_WORKBENCH.json

  REQUEST_FORGE
    TOOL_REQUESTS
    TOOL_SPECS

  FACTORY_NURSERY
    CANDIDATE_SCRIPTS
    CANDIDATE_FIXTURES
    CANDIDATE_RECEIPTS

  SCENT_ENGINE
    SEARCH_PROFILES
    SEARCH_RESULTS
    SEARCH_INDEX

  QUARANTINE_REVIEW
  PARKING
  EXPORTS
```

## First working tools

Build only seed-level, local-safe tools:

1. `OPEN_HOUSE_ROOT`
2. `OPEN_RESEARCH_PACK`
3. `SHOW_TOOL_REGISTRY`
4. `CHECK_LOOSE_FILES`
5. `HASH_SELECTED_FILE`
6. `SHOW_LATEST_RECEIPTS`
7. `ORGANIZE_RESEARCH_PACK_DRYRUN`
8. `CREATE_TOOL_REQUEST_CARD`

Write these as registry/tool-card entries first. Implement only what is safe and simple.

## Required rule

A script is not a tool until it has:

- registry row;
- tool card;
- allowed mode;
- forbidden powers;
- receipt expectation;
- DoesNotProve;
- stop line.

Hard rule:

`NO_TOOL_CARD / NO_BUTTON`

## Kernel requirement

Create a first-pass kernel that can accept a command object and route it through:

1. validate command shape;
2. load registry;
3. find tool row;
4. load tool card;
5. check allowed mode;
6. check forbidden powers;
7. run dry-run or safe action;
8. capture output;
9. write receipt;
10. append event row;
11. update current workbench;
12. return display object.

If full kernel is too much, create a stub with TODO sections and working dry-run path for one tool.

## First proof target

The first pudding should prove this loop:

`dock opens -> registry loads -> user selects ORGANIZE_RESEARCH_PACK_DRYRUN -> kernel checks card/policy -> dry-run runs -> receipt/event row written -> current workbench updates`

No run mode is required for first proof. Dry-run only is acceptable and preferred.

## Receipts required

Return a receipt with:

- chosen dock root;
- files created;
- files modified;
- files not touched;
- proof that ignored zones were not modified;
- any blocked items;
- any TODOs;
- SHA256 for main created files;
- first dry-run result if performed;
- final status.

## Stop lines

Stop if:

- staging root is unclear;
- build would touch research pack source files;
- build would touch Git;
- tool wants delete/move powers;
- generated tool would become active automatically;
- registry/tool-card mismatch appears;
- script cannot run in dry-run safely;
- any unknown file would need deletion;
- permissions or path state are unclear.

## Return format

Return:

1. Chosen root.
2. Summary of created files.
3. Receipt path.
4. Main file hashes.
5. Dry-run proof if any.
6. What does not prove.
7. Next legal action.

## Final mule carry line

Build the small dock seed, not the whole future. The first win is a safe local control room with a kernel, registry, tool cards, dry-run path, receipts, event ledger, and current workbench. Factory power stays parked until the dock can prove known tools safely.
