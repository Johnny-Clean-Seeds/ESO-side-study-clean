# START HERE - GROVE BRANCH

Status: `ACTIVE_POINTER / TREE_BRANCH / CLEAN_REBUILD`

Current outside-agent rule:

Before deep branch work, load the compact repo map:

`../ACTIVE_CHAT_DROPS/07_OUTSIDE_AGENT_PROJECT_MAP_AND_TOOL_ACCESS.md`

Then load the branch active packet. Use helper files and branch ledgers to target reads; do not bulk-load archives.

Use the active load surface here:

`ACTIVE/00_READ_FIRST.md`

Folder shape:

`ACTIVE` - clean working packet for this branch.

`MEDIA` - future screenshots, maps, photos, frame captures, PDFs, and visual source objects.

`ARCHIVE` - original draft files, raw capture, docx copies, source bank, and receipts.

This branch follows the same tree law as the Hall branch:

`one branch folder -> one active packet -> one media shelf -> one archive vault`

Do not start in `ARCHIVE` unless auditing or recovering exact old wording.

Current Grove next safe action:

`BATCH A1.2 - EARLY ADDRESS LOCK`

Keep Grove separate from Hall/Cooper/Mercy Hunter map work unless the current task explicitly asks for a comparison lane.
