# READ FIRST — SIDE-BY-SIDE REBUILD HOLD

Status: `ACTIVE_CONTROL / LOAD_FIRST / V2_REBUILD / HELD_FOR_USER_RELEASE`
Built: 2026-06-10
Scope: standalone packet only. No current project folder was modified.

## Why this V2 exists

The first clean packet was directionally correct but too compressed. The old research stack contains `engines.txt` at 7528 lines and `moving forwards.txt` at 1069 lines, while the V1 cleaned work file was only 244 lines. That is a preservation risk for this branch because older instructions, later freezes, and current next actions coexist in the source stack.

V2 fixes that by keeping two surfaces:

1. `LOAD SURFACE` — short enough for a new agent to use without drifting.
2. `PRESERVATION SURFACE` — exact source snapshots and side-by-side audit so no material is silently lost.

## Current live state

Latest accepted state from the source stack:

`BATCH 30 / FORK-008 — GREAT SEAL / CENTER SHIFT / NATIONAL EMBLEM RETURN`

Status:

`FROZEN / EMBLEM-RECODING CONFIRMED / LINEAGE BLOCKED / NOT_VIDEO_LOCKED`

Current next action:

`MASTER-MODEL-005 — CENTER-STACK / EMBLEM-RECODE REVISION`

## Anti-drift rule

Do not start from the old handoff’s `COMBO-RUN-001` line. That line is preserved as proof history. It is not the current next action.

Do not start from PATH-D, PATH-E, or PATH-G unless the user explicitly asks for review, redo, or audit.

Default action commands mean `RESEARCH RUN` and should run the current next action.

## Source voice split

Keep these voices separate:

- `Hall says` — symbolic grammar / esoteric synthesis.
- `Cooper reads/transmits` — noisy clue carrier.
- `Mercy Hunter shows` — video/editor/Christian-apocalyptic frame.
- `Archaeology verifies` — material culture, sites, objects, academic controls.
- `Official U.S. sources control` — Great Seal civic/design history.
- `Bible frames` — Mercy Hunter’s theological ending.
- `We infer` — bridges not directly stated by a source.

## Current safe model

`BLUE MAP = CONTINENTAL OBJECT-PATH COSMOGRAM / WORKING_SYNTHESIS`

It stacks object, path, site, emblem, and theology centers without claiming direct lineage.

Blocked:

- `SECC became the Great Seal`.
- `Great Seal is officially phoenix`.
- `Native symbols are evil`.
- `Ephesians proves archaeology`.
- `One literal Feathered Serpent cult migrated north and built the mounds`.
- `33rd parallel controls the map`.
- `Hall made the blue map`.
