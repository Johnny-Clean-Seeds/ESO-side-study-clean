# DRIFT AUDIT AND SUPERSESSION

Status: `ANTI_DRIFT_LEDGER / LOAD_IF_CONFLICTS_APPEAR`

## Drift found

The old source stack contains stale next actions. Most important: the old handoff tells an agent to run `COMBO-RUN-001 — Spiro Cosmogram + Cahokia Path of Souls`. Later work has already moved far beyond that.

Latest state supersedes the old handoff start.

## Superseded starts

`COMBO-RUN-001` = proof-history / already run and frozen.

`PATH-D` = already promoted/repaired as SECC style/object/motif bridge.

`PATH-E` = already frozen as Hall symbolic bridge with official-history firewall.

`PATH-G` = already frozen as Mercy Hunter Christian warfare frame, not archaeology proof.

`Bridge Court after every freeze` = blocked by Action-First patch and Bridge Court throttle.

`Ask for more same-video screenshots` = blocked; current-video frame custody is locally exhausted.

## Current next action

`MASTER-MODEL-005 — CENTER-STACK / EMBLEM-RECODE REVISION`

## Drift symptoms for future agents

A future agent is drifting if it:

- recaps the whole branch when user says `next`;
- restarts from `COMBO-RUN-001`;
- treats old handoff as newer than later freezes;
- runs Bridge Court automatically;
- invents new rooms without need;
- promotes the blue map without video custody;
- collapses Hall, Cooper, Mercy Hunter, archaeology, Bible, and official U.S. history into one voice;
- turns Mercy Hunter's Christian frame into archaeology;
- treats Hall's Great Seal phoenix reading as official history;
- lets 33rd-parallel material steer the model.

## Correct recovery

Return to:

`READ_FIRST → MASTER_WORK_SPINE → IDEA_REGISTRY → RULES_METHODS_ROOMS → MAP_LEDGER → AGENT_HANDOFF`

Then run only the current next action unless user asks for review or audit.
