# START HERE - CLEAN REBUILD POINTER

Status: `ACTIVE_POINTER / CLEAN_REBUILD / BUILT_FROM_DRAFT`

Current outside-agent rule:

Before deep branch work, load the compact repo map:

`../ACTIVE_CHAT_DROPS/07_OUTSIDE_AGENT_PROJECT_MAP_AND_TOOL_ACCESS.md`

Then load the branch active packet. Use helper files and branch ledgers to target reads; do not bulk-load archives.

Use the new active load surface here:

`ACTIVE/00_READ_FIRST.md`

The older draft packet, root run notes, media files, and old packet history are inside `ARCHIVE` or `MEDIA`. They are not deleted, but they are no longer the clean first-read path for this rebuild.

Current Hall next safe action:

`PART 6 OBJECT-CONTROL CLOSEOUT - GREEN-CROSS IMAGE / SIGNATURE PAGE / FENDLER TEXT`

Part 7 is staged only after Part 6 and must not be blended into Part 6.

Active clean-start file count:

`8 files in ACTIVE + this pointer = 9 active files`

That keeps the working surface below the 15-file ceiling while preserving the older draft material for audit and recovery.
